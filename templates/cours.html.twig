{# Evaluation d'expression (variable ou table en php) #}
{{ expressions }}
{# Evaluation des structures de contrôle (if, else, for) #}
{% condition %}
{# commentaire ne sont pas lus par le template#}

